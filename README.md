# Evala

A minimal Hugo news site focused on finance, government policy, and how public
decisions affect everyday life.

## Local Development

This site uses the PaperMod Hugo theme as a Git submodule.

```sh
git submodule update --init --recursive
hugo server --buildDrafts
```

Build the static site with:

```sh
hugo
```
